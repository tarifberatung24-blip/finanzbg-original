import { CircleAlert, FileCheck2, ShieldCheck } from "lucide-react"
import type { CanonicalTaxReturn2025 } from "@/lib/canonical-tax-model"
import { getPdfReadiness } from "@/lib/tax-pipeline"

export function TaxPipelineReview({ canonical }: { canonical: CanonicalTaxReturn2025 }) {
  const readiness = getPdfReadiness(canonical)
  return <section className="mt-8 rounded-2xl border border-border bg-card p-5 shadow-sm" aria-labelledby="tax-pipeline-review"><div className="flex items-start gap-3"><ShieldCheck className="mt-1 size-5 text-primary" /><div><p className="text-xs font-semibold uppercase tracking-[0.16em] text-primary">Canonical Tax Return</p><h2 id="tax-pipeline-review" className="mt-1 text-xl font-semibold text-foreground">Преглед и готовност</h2></div></div><p className="mt-3 text-sm leading-6 text-muted-foreground">Една структурирана данъчна истина, с произход на всяко поле и без непроверени предположения.</p><div className="mt-5 grid gap-3 sm:grid-cols-3"><div className="rounded-xl bg-muted/50 p-3"><p className="text-xs text-muted-foreground">Избрани формуляри</p><p className="mt-1 font-medium text-foreground">{readiness.forms.length}</p></div><div className="rounded-xl bg-muted/50 p-3"><p className="text-xs text-muted-foreground">Validation issues</p><p className="mt-1 font-medium text-foreground">{readiness.issues.length}</p></div><div className="rounded-xl bg-muted/50 p-3"><p className="text-xs text-muted-foreground">Състояние</p><p className="mt-1 font-medium text-foreground">{readiness.status}</p></div></div><div className="mt-5 space-y-2">{readiness.forms.map((form) => <div key={form.identifier} className="flex items-start gap-3 rounded-xl border border-border p-3"><FileCheck2 className="mt-0.5 size-4 shrink-0 text-primary" /><div><p className="text-sm font-medium text-foreground">{form.title}</p><p className="text-xs text-muted-foreground">{form.identifier} · {form.verificationStatus} · {form.mappingStatus}</p></div></div>)}</div>{readiness.issues.map((issue) => <div key={issue.code} className="mt-3 flex items-start gap-2 rounded-xl border border-amber-500/30 bg-amber-500/10 p-3 text-sm text-foreground"><CircleAlert className="mt-0.5 size-4 shrink-0 text-amber-600" />{issue.message}</div>)}</section>
}
